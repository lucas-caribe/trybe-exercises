const getUpperCaseString = (str) => str.toUpperCase();
const getFirstLetter = (str) => str[0];
const concatStrings = (a, b) => a + b;

module.exports = { getUpperCaseString, getFirstLetter, concatStrings };
